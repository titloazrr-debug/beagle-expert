import type { AlternativeBreed } from "@/types";
import raw from "../../content/alternative-breeds.json";

/** Placeholders multi-races (redirection future hors Beagle). */
export const alternativeBreeds: AlternativeBreed[] = raw as AlternativeBreed[];
